# WebGuard backend dependencies
# Pin major versions, allow patches

fastapi>=0.115.0,<0.120.0
uvicorn[standard]>=0.32.0,<0.40.0
python-multipart>=0.0.12,<0.1.0
pydantic>=2.9.0,<3.0.0

# Scanners
semgrep>=1.95.0
bandit>=1.7.10
detect-secrets>=1.5.0

# PDF report generation
reportlab>=4.2.0,<5.0.0

# Note: gitleaks is a Go binary, installed separately in the Dockerfile
